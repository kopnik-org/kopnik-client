export {};

declare global {
  // namespace fetch {
    // fetch:(url: string, options?: {}) => Promise<any>
    // function mockIf (match: RegExp, handler: (request: RequestInit)=>any):void;
  // }
}

// declare namespace fetch {
//   function mockIf(match: RegExp, handler: (request: RequestInit)=>any):void;
// }
